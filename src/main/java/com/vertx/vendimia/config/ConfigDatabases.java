package com.vertx.vendimia.config;

import io.vertx.core.Vertx;
import io.vertx.core.json.JsonObject;
import io.vertx.ext.mongo.MongoClient;

public class ConfigDatabases {

  private void setMongoConfiguration(Vertx vertx) throws Exception {
  /*  JsonObject config = new JsonObject();
    config.put("port", 7777);
    config.put("host", );
    config.put("db_name", apiConfiguration.getDatabase());
    config.put("username", apiConfiguration.getUsername());
    config.put("password", apiConfiguration.getPassword());
    config.put("useObjectId", true);
    MongoClient mongoClient = MongoClient.createNonShared(vertx, config);
    if (null == mongoClient) {
      throw new Exception("No se pudo establecer la conexión a la BD");
    }
    this.clienteMongo.setMongoClient(mongoClient);
  }*/
  }
}
